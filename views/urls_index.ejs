<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>
  <% for (let key in urls) { %>
  <form method="POST" action="/urls">
  </form>
  <form method="POST" action="/urls/<%= key %>/delete">
    <ul>
      <li>
        <%= urls[key].shortURL %> : <a style="color:#3b3a36;" href="<%= urls[key].longURL %>"><%= urls[key].longURL %></a>
        <a style="color:#3b3a36;" href="/urls/<%= key %>" id="edit">Edit</a>
        <button type="submit" class="button">Delete</button>
      </li>
  </ul>
  </form>
  <% }; %>
  <form method="GET" action="/urls/new">
    <button type="submit">Create new URL</button>
  </form>

</body>
</html>