<!DOCTYPE html>
<html lang='en'>
<head>
  <title>TinyApp</title>
</head>
<body>
  <header>
    <% include ./partials/_header %>
  </header>
  <ul>
    <% for (let urlID in shortURL) { %>
      <li><%= shortURL[urlID] %> : <%= urlID %></li>
    <% }; %>
  </ul>
</body>
</html>